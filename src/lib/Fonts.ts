/**
 * Fonts — Shared font family constants for Cloud Quest.
 *
 * Uses "Press Start 2P" (OFL licensed pixel art font from Google Fonts)
 * with monospace fallback for environments where the font can't load.
 */

/** Primary pixel art font for all game UI text. */
export const FONT_PIXEL = '"Press Start 2P", monospace';

/** Font family string for Phaser text objects. */
export const FONT_FAMILY = 'Press Start 2P, monospace';
