{
  "version": "v1",
  "hooks": [
    {
      "name": "Lint TypeScript on save",
      "trigger": "PostFileSave",
      "description": "Runs TypeScript compiler check after any TS file is saved to catch type errors immediately.",
      "matcher": "\\.(ts|tsx)$",
      "action": {
        "type": "command",
        "command": "npx tsc --noEmit --pretty 2>&1 | head -50",
        "timeout": 30
      }
    }
  ]
}