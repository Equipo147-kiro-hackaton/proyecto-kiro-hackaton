{
  "version": "v1",
  "hooks": [
    {
      "name": "Run tests after task completion",
      "trigger": "PostTaskExec",
      "description": "Runs all tests after a spec task is marked complete to ensure nothing is broken.",
      "action": {
        "type": "command",
        "command": "npx vitest --run 2>&1 | tail -20",
        "timeout": 60
      }
    }
  ]
}